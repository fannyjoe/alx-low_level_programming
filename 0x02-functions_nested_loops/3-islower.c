#include "main.h"

/**
 * _islower - checks if a character is lower.
 *
 * @c: the character to check.
 * Return: 1 on success, 0 otherwise
 */
int _islower(int c)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
		if (c == 'a')
		{
		_putchar('a');
		_putchar('H');
		}
	_putchar('\n');
	return (0);
}

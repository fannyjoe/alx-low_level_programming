#include <stdio.h>

/**
 * main - entry point.
 *
 * Return: always 0.
 */
int main(void)
{
	int i = 0;
	long int j = 1;
       long k = 2;
	long result;
 	
	while (k + j < 4000000)
	{
	k <= j;
	if (k % 2 == 0)
	result += k;
	j = k - j
	i++;
	}
	printf("%d\n", result);
	return (0);
}

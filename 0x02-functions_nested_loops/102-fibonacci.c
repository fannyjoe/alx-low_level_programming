#include <stdio.h>

/**
 * main - entry point.
 *
 * Return: always 0.
 */
int main(void)
{
	int a = 0;
	int b = 1;
	int c = 2;

	for (a = 0; a < 50; a++)
	{
		if (a == 0)
		{
		printf(", %d", b);
		}
		else if (a == 1)
		{
		printf(", %d", c);
		}
		else
		{
			k += j;
			j = k - j;
		printf(", %d", k);
		}
	}
	printf("\n");
	return (0);
}

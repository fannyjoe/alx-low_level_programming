#include "main.h"

/**
 * _abs - calculate the absolute value of an integer.
 *
 * @j: integer to compute.
 * Return: always 0.
 */
int _abs(int j)
{
	if (j < 0)
	{
		j = -(j);
	}
	else (j >= 0)
	{
		j = j;
	}
	return (0);
}

